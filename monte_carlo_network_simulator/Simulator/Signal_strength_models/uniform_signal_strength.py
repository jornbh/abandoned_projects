import numpy as np

#Implies a circle when there is only one receiver
# Or all nodes having the same distance from eachother 



def uniformSignalStrength( fromID, toID, disturber):
    return 0.25 # Just returns 0.5 ==> All nodes have the same distance form each other