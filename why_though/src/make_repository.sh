# Just a set of commands to make a git clone in the correct folder, and running the requred setup 
cd ~
cd Documents
mkdir .set_backgrounds
cd .set_backgrounds
git clone https://github.com/jornbh/why_though.git
cd why_though/src
bash setup.sh
